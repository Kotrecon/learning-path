# Прогресс: Модуль 3 — Configuration Pipeline

> Цель: Настроить конфигурацию с приоритетами: CLI > ENV > JSON.

---

## Секция 1: Базовый провайдер appsettings.json

### [x] Задача 3.1: Настроить чтение конфигурации из appsettings.json

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module3.1): clean Program.cs, keep only config reading`
- **Заметки/Наблюдения:**
  - JSON «сплющивается» в плоские ключи с разделителем `:`
  - `builder.Configuration` доступен до `Build()`, что удобнее резолва из `host.Services`
  - 💡 Инсайт: `IConfiguration` индекcатор возвращает `null` при отсутствии ключа, а не кидает ошибку.

---

## Секция 2: Переопределение через переменные окружения

### [x] Задача 3.2: Реализовать приоритет ENV-переменных над JSON

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module3.2-3): verify ENV and CLI config precedence`
- **Заметки/Наблюдения:**
  - Двойное подчёркивание `__` в ENV автоматически маппится на `:` при загрузке
  - Провайдер ENV регистрируется после JSON, поэтому переопределяет значения
  - 💡 Инсайт: ENV идеальны для секретов и средовых настроек, так как не попадают в VCS.

---

## Секция 3: Наивысший приоритет аргументов командной строки

### [x] Задача 3.3: Верифицировать приоритет CLI-аргументов над ENV и JSON

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module3.2-3): verify ENV and CLI config precedence`
- **Заметки/Наблюдения:**
  - CLI-провайдер добавляется в цепочку последним, поэтому имеет наивысший приоритет
  - Синтаксис `--Key=Value` работает стабильно, требует `--` после `dotnet run`
  - 💡 Инсайт: CLI удобен для `one-off` запусков и отладки, но не подходит для долгосрочного хранения настроек.

---

## Секция 4: Автоматизация проверки приоритетов

### [x] Задача 3.4: Создать скрипт для автоматической проверки цепочки приоритетов

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module3.4): add precedence verification script`
- **Заметки/Наблюдения:**
  - Скрипт `Scripts/test-config-precedence.ps1` использует `Push-Location` для корректного CWD
  - `--no-build` ускоряет прогон, `2>&1 | Out-String` гарантирует стабильный парсинг regex
  - 💡 Инсайт: Автоматизация конфигов исключает человеческий фактор и гарантирует CI-совместимость.

---

## Секция 5: Типизированный доступ через IOptions<T>

### [ ] Задача 3.5: Настроить биндинг конфигурации в типизированный объект

- **Статус:** ⏳
- **Дата выполнения:**
- **Коммит:**
- **Заметки/Наблюдения:**

---

## Секция 6: Документация и сдача модуля

### [ ] Задача 3.6: Подготовить документацию и открыть PR для сдачи модуля

- **Статус:** ⏳
- **Дата выполнения:**
- **Коммит:**
- **Заметки/Наблюдения:**

---

## ✅ Критерий приёмки модуля

- [ ] `Program.cs` использует `Host.CreateApplicationBuilder(args)` (современный паттерн .NET 8/10)
- [ ] `appsettings.json` читается через `IConfiguration` с разделителем `:`
- [ ] Переменная окружения `App__Timeout` (с `__`) переопределяет значение из JSON
- [ ] Аргумент командной строки `--App:Timeout=300` имеет наивысший приоритет
- [ ] Скрипт `test-config-precedence.ps1` автоматизирует проверку и возвращает код `0`
- [ ] `IOptions<AppSettings>` корректно биндит значения из конфигурации
- [ ] Все изменения зафиксированы в Git с понятной историей коммитов

---

📝 **Правила ведения:** Отмечайте `[x]` только после полной сдачи задачи. В поле `Заметки` пишите только технические факты, ошибки и инсайты. Коммиты указывайте в формате `sha` или `feat(module3): ...`.
