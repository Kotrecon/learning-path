# Прогресс: Модуль 4 — Options & Runtime Reconfiguration

> Цель: Подписаться на изменения конфига, применять изменения без рестарта.

---

## Секция 1: `IOptionsMonitor<T>` — реактивный доступ к конфигурации

### [x] Задача 4.1: Настроить сервис с IOptionsMonitor для live-обновлений

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module4.1): implement IOptionsMonitor<T> with hot-reload support`
- **Заметки/Наблюдения:**
  - `reloadOnChange: true` обязателен для отслеживания изменений файла
  - `CurrentValue` возвращает актуальное значение при каждом обращении
  - 💡 Инсайт: `IOptionsMonitor` не «магия» — он просто подписывается на `IChangeToken` от провайдера конфигурации.

---

## Секция 2: Подписка на OnChange для hot-reload

### [x] Задача 4.2: Реализовать callback OnChange для обработки изменений конфига

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module4.2): subscribe to OnChange events with proper disposal`
- **Заметки/Наблюдения:**
  - `OnChange` возвращает `IDisposable` — в долгоживущих сервисах его нужно хранить и вызывать `Dispose()` при остановке
  - Коллбэк выполняется на потоке провайдера: делать его коротким и неблокирующим
  - 💡 Инсайт: событийная модель эффективнее поллинга. Для защиты от «шума» файловой системы можно добавить идемпотентность или debounce, но для базового сценария достаточно простой подписки.

---

## Секция 3: Валидация конфигурации при старте

### [x] Задача 4.3: Настроить валидацию конфига через DataAnnotations и ValidateOnStart

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module4.3): add DataAnnotations validation with ValidateOnStart`
- **Заметки/Наблюдения:**
  - `ValidateOnStart()` гарантирует fail-fast: приложение не запустится с плохим конфигом
  - Атрибуты `[Required]`, `[Range]` проверяются автоматически через `ValidateDataAnnotations()`
  - ⚠️ **Важно**: для работы `ValidateDataAnnotations()` требуется пакет `Microsoft.Extensions.Options.DataAnnotations` (добавляется через `dotnet add package Microsoft.Extensions.Options.DataAnnotations`)
  - 💡 Инсайт: Валидация на старте дешевле, чем обработка `NullReferenceException` в середине бизнес-процесса.
  - `.BindConfiguration("App")` — более чистая альтернатива `.Bind(GetSection("App"))` в .NET 8/10

---

## Секция 4: Интеграция hot-reload в бизнес-логику

### [x] Задача 4.4: Реализовать сервис, применяющий изменения конфига в рантайме

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module4.4): implement WorkerService with runtime config application`
- **Заметки/Наблюдения:**
  - `AddHostedService<T>` + `BackgroundService` — стандартный паттерн для фоновых задач в Generic Host
  - `_monitor.CurrentValue` на каждой итерации цикла гарантирует актуальность конфига без ручного кэширования
  - ✅ `ValidateOnStart()` + `ValidateDataAnnotations()` работают и на старте, и при рантайм-изменении файла (fail-fast при invalid-конфиге)
  - ✅ Shutdown-механизм: хост отменяет `CancellationToken` → цикл выходит → `StopAsync()` вызывается автоматически в обратном порядке регистрации
  - 💡 Инсайт: hot-reload — это дисциплина чтения. Если значение нужно прямо сейчас — спрашивай `CurrentValue`, не сохраняй копию в полях класса.

---

## Секция 5: `IOptionsSnapshot<T>` для per-scope конфигурации

### [x] Задача 4.5: Исследовать `IOptionsSnapshot<T>` для изоляции конфигурации по скоупам

- **Статус:** ✅ Completed
- **Дата выполнения:** 2026-05-27
- **Коммит:** `feat(module4.5): demonstrate IOptionsSnapshot<T> per-scope isolation`
- **Заметки/Наблюдения:**
  - `IOptionsSnapshot<T>` автоматически регистрируется при `Configure<T>()`, дополнительной настройки не требует
  - Значение замораживается на момент создания скоупа, что гарантирует консистентность внутри операции
  - При изменении конфига новый скоуп получит обновлённые значения, старый — нет
  - ⚠️ Нельзя инжектить в Singleton-сервисы (captive dependency → утечка памяти/устаревшие данные)
  - 💡 Инсайт: Snapshot — это не hot-reload. Это гарантия стабильности конфига на время транзакции/запроса.

---

## Секция 6: Документация и сдача модуля

### [] Задача 4.6: Подготовить документацию и открыть PR для сдачи модуля

- **Статус:** ⏳ In Progress
- **Дата выполнения:**
- **Коммит:**
- **Заметки/Наблюдения:**

---

## ✅ Критерий приёмки модуля

- `IOptionsMonitor<AppSettings>` инжектится в сервис и предоставляет `CurrentValue`
- `OnChange` callback вызывается при изменении `appsettings.json` без перезапуска приложения
- Валидация через `ValidateDataAnnotations` и `ValidateOnStart` блокирует запуск при некорректном конфиге
- Сервис применяет новое значение конфига в рантайме после изменения файла
- `module4/README.md` содержит примеры кода и инструкции по проверке hot-reload
- Все изменения зафиксированы в Git с понятной историей коммитов

---

📝 **Правила ведения:** Отмечайте `[x]` только после полной сдачи задачи. В поле `Заметки` пишите только технические факты, ошибки и инсайты. Коммиты указывайте в формате `sha` или `feat(module4): ...`.
